# Dashboard URL (para enviar predicciones vía --aws-url)
# Si no se define, se puede pasar como argumento CLI
DASHBOARD_URL=http://localhost:5000

# Puerto serial y baudrate por defecto
SERIAL_PORT=/dev/ttyUSB0
SERIAL_BAUD=460800
